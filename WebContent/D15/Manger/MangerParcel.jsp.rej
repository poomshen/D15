diff a/WebContent/D15/Manger/MangerParcel.jsp b/WebContent/D15/Manger/MangerParcel.jsp	(rejected hunks)
@@ -9,10 +9,10 @@
 </head>
 <body>
  <c:set var="list" value="${requestScope.parcellist}" />
- <c:forEach var="list" items="${list }">
+ <c:forEach var="list" items="${list}">
  <div>
  	${list.id } / ${list.name } / ${list.phone } / ${list.email } /${list.addr } /${list.petOk } /
- 	${list.reqdate } / ${list.begdate } /${list.enddate } / <img alt="" src=""${list.img }> /
+ 	${list.reqdate } / ${list.begdate }/ <img alt="" src=""${list.img }> /
  	${list.code } /${list.count } /${list.date }
  </div>
  </c:forEach>
